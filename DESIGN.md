# 《周处除三害·新心灵舍》第一人称游戏 — 设计方案 v1

> 影迷同人致敬作品（unofficial fan game）。不含任何电影画面、音乐、剧照素材；
> 全部美术为程序化生成的低多边形几何体，全部音效为 WebAudio 实时合成。
> 与片方及任何权利人无关联。

---

## 交付状态（v0.4）

第三章「新心灵舍」已完整实现并可通关，含电影结局段的码头自首与刑场。

- 线上：<https://li-mingshuang.github.io/zhou-chu-san-hai/>
- 十一个节拍全部可玩，三种结局分支齐全。
- 自动化验收：`pnpm verify`（十五机位）、`pnpm entry`（真实入口）、
  `pnpm look`（角色可见性像素比对）、`pnpm frames`（九个细节帧）、
  `pnpm play`（自动通关）全部通过，本地与线上地址各跑一遍。
- 规模：draw call 17–454，三角形 1.9 万–8.3 万，客户端产物体积约 700 kB（gzip 191 kB）。
- 尚未实现：第一、二章（通缉榜上的另外两害）；手柄与移动端。

### v0.4：把截图变成能读的简笔画，然后照出了一堆姿态问题

`tools/sketch.mjs` 用 Canny 那一套（自动色阶 → 高斯 → Sobel → 非极大值抑制 →
滞后阈值）求边缘，再用**盲文点阵**（U+2800–U+28FF）渲染：一个字符装 2×4 个点，
108 列文本就有 216×144 的有效分辨率。配合 `--vs 另一张.png` 可以把单个角色
从画面里"抠"出来单独看形状。

它照出的问题：

1. **姿态符号整体反了。** 骨架本地朝 -Z，垂下的肢体绕 X 转 θ 后指向
   `(0, -cosθ, -sinθ)`——正角度才是往前。原本 `sit/kneel/reach/handsUp/sweep/strum`
   全是负角度：信徒把手伸到背后劝你，扫地时上身往后仰，头往上抬。
2. **简化骨架没法坐。** 信徒原本用不带膝关节的合并腿，一坐下脚就穿到地板下面 33 厘米。
   改成带膝关节的骨架之后，draw call 从 337 涨到 454。
3. **唱歌的人悬在离地半米。** 他放在讲台范围之外，却给了台面高度。
4. **扫帚整柄埋进地里半米。**
5. **人物没有落地感。** 加了程序化径向渐变的接触阴影。
6. **第一人称的枪浮在空中。** 补了两只手与两条前臂，并把枪抬起来一点避免握把被切掉。
7. **俯视镜头太高。** 15.5 米时一个 1.78 米的人只占画面 5.9%（33 像素），
   改成 9 米 + 56° 俯角，并给镜头加了避障——逃跑是从礼厅门口开始的，
   不处理的话镜头会直接从屋顶穿出去。
8. 顺带发现分镜模式下没有关胶片颗粒，所有像素差分都被逐帧噪点污染。

### v0.3 修掉的三处"接线没接上"

真机试玩暴露的问题，都是截图与分镜验收看不见的：

1. **标题卡不消失。** `#title-card` 被 JS 加了 `.hidden` 类，但样式表里没有对应规则，
   元素照旧占着版面；而 `#overlay` 已经被改成透明，于是标题浮在游戏画面上。
   修法：补上规则，并且 `setCardVisible` 同时写内联 `display`，不再依赖样式表。
2. **左键打不出枪。** 指针锁只在"用户手势"里被允许，而 `startRun()` 先 `await` 了
   音频与超时竞速、之后才请求锁，手势窗口可能已经过期；拿不到锁时鼠标按键又被
   `if (!this.locked) return` 静默丢弃。修法：把请求锁移到第一个 await 之前同步执行，
   没锁时左键先用来夺回锁，并对"浏览器就是不给锁"的情况做降级——鼠标视角与开火照常。
3. **HUD 有大半没接上线。** `setLetterbox` / `setCrosshair` / `setAmmo` / `setCinematic`
   定义了却从来没被调用过，于是准星与弹药显示从未出现过。修法：每帧 `syncHud()`，
   带脏检查。

另外补上真人会期待的三个人：门口**扫地**的、礼厅里**弹吉他唱歌**的、以及把
**尊者（老大）** 移到讲台最前沿并加一束顶光——进门第一眼就该看见他。

---

## 0. 一句话

一款**第一人称剧情射击**游戏：你扮演陈桂林，在澎湖小岛的"新心灵舍"里，
从被骗的信徒走到礼堂中央，拔出那把枪。
**镜头会在关键时刻自己动**——俯视跑步、侧视横移、电影定格机位，
像一部你可以走进去的电影。

首版交付：**第三章「新心灵舍」**（全片高潮，小岛枪战）可玩纵向切片。

---

## 1. 原作脉络（设计依据）

| 图腾 | 人物 | 影片位置 | 游戏化 |
|---|---|---|---|
| 鸽（痴） | 陈桂林 | 全片 | 玩家自身 |
| 蛇（嗔） | 香港仔 | 第二幕 | 规划中的第二章 |
| 猪（贪） | 林禄和／尊者 | 第三幕·澎湖小岛 | **本次实现** |

第三幕关键节点（本作章节节拍来源）：

1. 乘船抵岛，沿石阶上山，被"师兄"迎入**新心灵舍**。
2. 交出随身财物，"洗涤心灵"，参加礼堂仪式，被当众羞辱、忏悔、挨鞭。
3. 信仰崩塌：发现**密室**——成箱现金、名表、被锁住的孩子、满墙信徒来信。
4. **礼堂名场面**：他回到最后一排坐下，起身，掏枪，逐一清除不肯离开的信徒。
5. 逃离下山，海边洗去血污，登船自首。
6. 结局字幕：行刑。

游戏不做原作剧情的复述型过场，而是把**玩家放进第 4 步的沉默里**。

---

## 2. 核心体验目标

三个关键词：**冷、慢、被观看**。

- **冷**：颜色是骗人的暖黄。礼堂里点着灯笼，密室是惨绿日光灯，海边是过曝的白。
  开枪不给爽感反馈——只有后坐力、耳鸣、和一片更安静的空气。
- **慢**：这不是枪战游戏。没有掩体、没有血量、没有准星跳弹。
  你有 6 发子弹和一个漫长的礼厅。**主题是扣扳机的节奏，不是命中率。**
- **被观看**：镜头经常不是你。你会被推上过肩位、俯视位、侧位——
  因为你正在做的事，本来就该被人看着。

---

## 3. 视角导演系统（本作最大特色）

`CameraDirector` 统一管理镜头，按**节拍脚本 + 触发体**切换，切换时做
crossfade + 时间拉伸（0.35s，慢动作）过渡。四种镜头模式：

| 模式 | 用途 | 表现 |
|---|---|---|
| `FirstPerson` | 默认。走路、对话、仪式、密室、开枪 | PointerLock 自由视角，头部 bob，呼吸摇摆；拔枪时 FOV 收缩、准星浮现 |
| `TopDownRun` | **俯视跑步**：逃离新心灵舍、沿山径冲向海边 | 高度 14m、俯角 62° 的第三人称俯视；角色为低模剪影；镜头带惯性延迟跟随 + 转向侧倾；速度线 + 屏幕边缘拉伸；脚步声变沉 |
| `SideScroll` | 横版侧视：海边洗血、码头、行刑 | 正交侧视，固定推进轨道，玩家只控制左右 |
| `Cinematic` | 电影定格机位：开场、密室真相、结局卡 | 固定／轨道机位，玩家失去控制，HUD 隐没；字幕独白；可跳过（长按 Esc） |

**切换是"被允许"的**：镜头切换从不打断玩家的操作意图（`TopDownRun` 仍可 W/A/S/D 跑），
只改变"谁在看"。切换计划由章节脚本声明：

```ts
director.run([
  { at: 'beat4.hall-enter',  mode: 'FirstPerson', fade: 0 },
  { at: 'beat4.first-shot',  mode: 'Cinematic',   hold: 2.4 },   // 定格：他抬手
  { at: 'beat4.walk-aisle',  mode: 'FirstPerson', fade: 0.35 },
  { at: 'beat6.escape',      mode: 'TopDownRun',  fade: 0.5 },   // 俯视跑步
  { at: 'beat7.shore',       mode: 'SideScroll',  fade: 0.8 },
]);
```

---

## 4. 玩法机制

- **移动**：WASD 走 / Shift 跑 / Ctrl 蹲 / Space 跨。无跳跃（保持"人"的重量感）。
- **交互**：E 查看。可交互物有**独白**——马桶上的血、女孩的作业本、满墙的信、
  香炉里没烧完的纸。这些是叙事的主要载体，不靠过场。
- **手枪**（本章核心）：
  - 初始 **6 发**，无备用弹（原作里他不需要更多）。
  - 第一人称射击：raycast 命中，弹道拖尾、弹壳抛落、烟。
  - **仪式瞄准**：按住右键进入，画面去饱和 + 环境音压低 + 心跳；
    松开即射击。这个"慢下来"的动作本身就是表演。
  - 后坐力：镜头抬 2.5°，准星恢复 0.4s。**不做命中数字、不做血雾**。
- **信徒 AI**（不是敌人）：三态状态机
  - `Exhort`：向你走来，伸手，反复说"师兄，把枪放下"。
  - `Kneel`：跪回原位，闭眼，"感谢天地"。
  - `Flee`：尖叫着跑出礼堂。
  - 关键设计：**你不是必须开枪**。每个信徒是独立个体，有走有留。
    全部放过是合法通关路径，结束文本不同（"你只是举着枪站在那里"）。
- **清算计数**：HUD 只有一行极小的字：`已除 0 / 3`。
  第 3 个格子永远留给"第三害"——按剧情，那是你自己。
- **存档**：localStorage 记录章节与节拍，可从任意节拍重开。

---

## 5. 章节节拍表（第三章）

| # | 节拍 | 镜头 | 玩法 | 时长 |
|---|---|---|---|---|
| 1 | 抵岛·石阶 | Cinematic → FPS | 无操作，走路即可；迎客的"师兄" | 60s |
| 2 | 前院·交财物 | FPS | 交互：交出手表（奶奶的遗物）——**唯一强制交互** | 90s |
| 3 | 礼堂仪式 | FPS | 坐着（锁定视角）、被点名、被鞭；字幕"感谢天地" | 120s |
| 4 | 密室真相 | FPS → Cinematic | 打开后山门、下楼梯、看见钱、看见孩子；**定格** | 90s |
| 5 | 回到礼堂 | FPS | 走回最后一排，坐下。**自己站起来** | 60s |
| 6 | **开枪** | FPS / Cinematic | 6 发子弹，逐一决断；信徒三态反应 | 150s |
| 7 | 逃离·俯视跑步 | **TopDownRun** | 从山上俯视着跑，穿过椰林与同伴的尸体；不设失败 | 70s |
| 8 | 海边·侧视 | SideScroll | 洗脸、剃须、点烟；走上船 | 60s |
| 9 | 自首 | FPS → **正面固定机位** | 码头上站着一排警察；他举手走过去 | 70s |
| 10 | 刑场 | **正面固定机位** | 三个月后。水泥、铁丝网、行刑柱 | 70s |
| 11 | 结局卡 | Cinematic | 字幕 + 计数回放；分支文本 | 30s |

合计约 **17 分钟**。

自首与刑场用的是**正面固定机位**——全片他第一次被人从正面看着，
和礼厅那场由他自己掌握视线的第一人称正好相反。刑场那一拍几乎去掉全部颜色。

---

## 6. 技术方案

- **栈**：Vite + TypeScript + three.js（无其他运行时依赖）。
- **美术**：程序化低多边形 + 平面着色 + 顶点色，零贴图零素材。
- **后处理**（自研单 pass）：低分辨率渲染目标（0.6×）→ 抖动 dither → 色差 → 暗角
  → 胶片颗粒 → 轻微分色。营造"低成本胶片"质感，同时压住性能开销。
- **音频**：WebAudio 全合成。枪声＝噪声爆破 + 低通扫频 + 卷积混响；脚步按材质分桶；
  环境＝海风噪声 + 蝉鸣带通 + 礼堂诵经低频 pad + 木质共振。
- **物理**：不使用物理引擎。碰撞用 AABB + 圆柱体滑动，射击用 raycast。
- **性能目标**：1080p / 60fps / 单个 draw call 数量 < 300（几何体合并 + InstancedMesh）。

### 目录结构

```
zhou-chu-san-hai/
  DESIGN.md  README.md  LICENSE  index.html
  package.json  tsconfig.json  vite.config.ts
  .github/workflows/deploy.yml      # GitHub Pages 自动发布
  src/
    main.ts
    core/      Game, Time, Input, EventBus, Save, Settings
    render/    Renderer, PostFX, Palette, Mats, Geometry(合并/实例化工具)
    camera/    CameraDirector + modes/{FirstPerson,TopDownRun,SideScroll,Cinematic}
    world/     islands.ts, Hall.ts, Yard.ts, Path.ts, Vault.ts, Shore.ts, props.ts
    entities/  Player.ts, Pistol.ts, Cultist.ts, Idol.ts, Child.ts
    systems/   Interaction, Dialogue, Objective, Combat, CultistAI, Trigger
    chapter3/  script.ts (节拍图), beats/*.ts
    ui/        Hud, Crosshair, Subtitles, Objectives, Menu, Ending
    audio/     AudioEngine, Synth, Sfx, Ambience
  tools/       shots.mjs  (headless Chrome 分镜截图验证)
```

### 可验证性（重要）

- `pnpm typecheck` + `pnpm build` 必须零错误。
- `?debug=1`：叠加 FPS / 当前节拍 / 镜头模式 / 玩家坐标。
- `?shot=<beat>&t=<sec>`：**分镜模式**。加载后自动把玩家与镜头放到该节拍的机位，
  冻结时间在第 t 秒，不响应输入。配合 `chrome --headless --screenshot` 批量出图，
  我逐张核对画面（构图、光照、取景），不靠"应该没问题"。
- 无失败状态：任何节拍都可 `?beat=N` 直接跳入，便于逐段验收。

---

## 7. 开发里程碑

| M | 内容 | 验收 |
|---|---|---|
| M1 | 脚手架 + 渲染 + PS1 后处理 + FPS 控制器 + 输入 | 能在空场走动的截图 |
| M2 | 程序化世界：礼堂／前院／石阶／密室／山径／海边 + 道具 | 各场景截图 |
| M3 | CameraDirector 四模式 + 过渡 | 切镜截图 + 手感自测 |
| M4 | 交互／独白／字幕／目标；手枪射击全套反馈 | 开枪截图 + 参数日志 |
| M5 | 章节节拍脚本 + 信徒 AI + 结局分支 | 从节拍 1 到 9 全程跑通 |
| M6 | 音频合成 + HUD + 菜单 + 存档 | 默认静音友好、字幕齐全 |
| M7 | 打磨：手感、光照、性能、可访问性（字幕／灵敏度／去抖） | 60fps 达标 |
| M8 | README（中英）／LICENSE／Pages workflow／建仓推送 | 线上 URL 可玩 |

---

## 8. 版权与边界声明

- 不使用任何电影素材（画面、剧照、海报、音乐、台词录音）。
- 台词为**重写**的简短独白与场景化对白，非逐字复制；字幕长度控制在必要范围。
- 暴力表现为**低多边形、非血腥**的抽象处理：命中即倒下，无血雾、无伤口特写。
- 游戏提供"不开枪"的完整路径，并在结局呈现对暴力的反思语气，而非奖励杀戮。
- 仓库 README 首屏标注 unofficial fan work，未获授权，非商业用途。
